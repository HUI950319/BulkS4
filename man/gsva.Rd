% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsva.R
\name{gsva}
\alias{gsva}
\alias{gsva,BulkRNAseq-method}
\title{Gene Set Variation Analysis (GSVA) for BulkRNAseq Objects}
\usage{
gsva(
  object,
  geneSet_name = "H",
  gsva_method = c("gsva", "ssgsea"),
  kcdf = "Gaussian",
  min_sz = 1,
  max_sz = Inf,
  mx_diff = TRUE,
  tau = 1,
  ssgsea_norm = TRUE,
  run_limma = TRUE,
  verbose = TRUE
)

\S4method{gsva}{BulkRNAseq}(
  object,
  geneSet_name = "H",
  gsva_method = c("gsva", "ssgsea"),
  kcdf = "Gaussian",
  min_sz = 1,
  max_sz = Inf,
  mx_diff = TRUE,
  tau = 1,
  ssgsea_norm = TRUE,
  run_limma = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A BulkRNAseq object containing expression data}

\item{geneSet_name}{Character vector, names of gene sets to analyze. Default "H" (Hallmark)}

\item{gsva_method}{Character, GSVA method to use. Options: "gsva", "ssgsea". Default "gsva"}

\item{kcdf}{Character, kernel to use for density estimation. 
Options: "Gaussian", "Poisson", "none". Default "Gaussian"}

\item{min_sz}{Integer, minimum gene set size. Default 1}

\item{max_sz}{Integer, maximum gene set size. Default Inf}

\item{mx_diff}{Logical, whether to use max difference. Default TRUE}

\item{tau}{Numeric, tau parameter for ssGSEA. Default 1}

\item{ssgsea_norm}{Logical, whether to normalize ssGSEA scores. Default TRUE}

\item{run_limma}{Logical, whether to run limma differential analysis. Default TRUE}

\item{verbose}{Logical, whether to print progress messages. Default TRUE}
}
\value{
Updated BulkRNAseq object with GSVA results stored in gsva slot
}
\description{
Perform Gene Set Variation Analysis using GSVA package
}
\examples{
\dontrun{
# Basic GSVA analysis
bulk_obj <- gsva(bulk_obj, geneSet_name = "H")

# ssGSEA analysis
bulk_obj <- gsva(bulk_obj, geneSet_name = "H", gsva_method = "ssgsea")

# Multiple gene sets
bulk_obj <- gsva(bulk_obj, geneSet_name = c("H", "C2_CP_KEGG"))

# Custom parameters
bulk_obj <- gsva(bulk_obj, 
                 geneSet_name = "H",
                 kcdf = "Poisson",
                 min_sz = 10,
                 max_sz = 200)
}

}
