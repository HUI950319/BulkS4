% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S4class_def.R
\name{BulkRNAseq}
\alias{BulkRNAseq}
\title{Create BulkRNAseq Object}
\usage{
BulkRNAseq(counts, metadata = NULL, add_msig.geneSet = TRUE)
}
\arguments{
\item{counts}{A matrix or data.frame of raw counts, or a list containing
required fields (counts, metadata, exprSet, allDiff)}

\item{metadata}{A data.frame with sample metadata. If NULL, an empty
data.frame will be created}

\item{add_msig.geneSet}{Logical. Whether to add MSigDB gene sets to the object}
}
\value{
A BulkRNAseq S4 object
}
\description{
Constructor function for BulkRNAseq S4 objects
}
\examples{
# Create from matrix
counts_mat <- matrix(rpois(1000, 10), nrow = 100, ncol = 10)
rownames(counts_mat) <- paste0("Gene", 1:100)
colnames(counts_mat) <- paste0("Sample", 1:10)

metadata <- data.frame(
  condition = rep(c("Control", "Treatment"), each = 5),
  row.names = colnames(counts_mat)
)

bulk_obj <- BulkRNAseq(counts_mat, metadata)
}
